package com.hackduke.codereggie.actions;

import com.google.appengine.api.search.Document;
import com.google.appengine.api.search.Field;
import com.google.appengine.api.users.User;
import com.google.appengine.api.users.UserServiceFactory;

public class KeywordDocument {
	
	public void createKeywordDocument() {
		/*Document doc = Document.newBuilder()
		    .addField(Field.newBuilder().setName("eventID").setText("the rain in spain"))
		    .addField(Field.newBuilder().setName("email")
		        .setText(currentUser.getEmail()))
		    .addField(Field.newBuilder().setName("domain")
		        .setAtom(currentUser.getAuthDomain()))
		    .addField(Field.newBuilder().setName("published").setDate(new Date()))
		    .build();
		*/
	}
}